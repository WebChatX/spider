import SpiderClientOptions from "./SpiderClientOptions";

class SpiderClientEngine {
  constructor() {
    // 初始化引擎
    this._initEngine(options);
  }

  /**
   *
   * @param {SpiderClientOptions} options
   */
  _initEngine(options) {
    // this.engine = new WebSocket();
  }
}

export default SpiderClientEngine;
